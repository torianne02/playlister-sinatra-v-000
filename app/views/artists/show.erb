<!-- <h1><%= @artist.name %></h1>

<h2>Song(s):</h2>

  <% @artist.songs.each do |song| %>
    <label>Song:</label> <a href="/songs/<%= song.slug %>"><%= song.name %></a>
      <label>Genre(s):</label>
      <ul>
        <% @song.genres.each do |genre| %>
          <li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
      </ul>
  <% end %>


  <h1><%= @artist.name %></h1>

  <h2>Song(s)</h2>

  <% @artist.songs.each.with_index(1) do |song,i| %>

    <label>Song <%= i %> Name: <a href="/songs/<%= song.slug %>"><%= song.name %></a></label>

    <ul>
      <label>Song's Genre(s)</label>
      <ul>
        <% song.genres.each do |genre| %>
          <li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
          <% end %>
        </ul>
    </ul>
  <% end %> -->

<ul>
  <% @artist.songs.each do |song| %>
    <li><a href="/songs/<%= song.slug %>"><%= song.name %></a></li>
  <% end %>
</ul>

<ul>
  <% @artist.genres.each do |genre| %>
    <li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
  <% end %>
</ul>